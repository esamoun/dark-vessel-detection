"""Matching must not manufacture dark vessels.

To cover: a detection matching an interpolated position within tolerance is matched; a stale
uninterpolated position does not produce a false dark vessel; tolerance boundaries behave.
"""
